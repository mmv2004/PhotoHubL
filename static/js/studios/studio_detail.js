document.addEventListener('DOMContentLoaded', function () {
    // Настройки для лайтбокса
    lightbox.option({
        resizeDuration: 200,
        wrapAround: true,
        albumLabel: 'Изображение %1 из %2'
    });
});
